<?xml version="1.0" encoding="utf-8"?>
<!-- Free, 1x1. One layout for both states (like widget_today_habits.xml's
     empty state) rather than a separate "unconfigured" tree — there's only
     ever one thing to hide or show. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/streak_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_card_background">

    <LinearLayout
        android:id="@+id/streak_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="8dp">

        <TextView
            android:id="@+id/streak_number"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/space_grotesk_medium"
            android:textColor="@color/tide_lantern"
            android:textSize="28sp" />

        <ImageView
            android:id="@+id/streak_status"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:layout_marginTop="2dp"
            android:layout_marginBottom="4dp" />

        <TextView
            android:id="@+id/streak_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="@font/manrope_medium"
            android:gravity="center"
            android:maxLines="1"
            android:textColor="@color/tide_silt"
            android:textSize="11sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/streak_unconfigured"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:fontFamily="@font/manrope_medium"
        android:gravity="center"
        android:padding="12dp"
        android:text="@string/widget_pick_a_habit"
        android:textColor="@color/tide_silt"
        android:textSize="12sp"
        android:visibility="gone" />
</FrameLayout>
