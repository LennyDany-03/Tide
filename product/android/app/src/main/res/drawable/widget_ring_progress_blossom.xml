<?xml version="1.0" encoding="utf-8"?>
<!-- Generated by tool/widget_palettes_test.dart from the Midnight original. -->
<!-- Weekly Recap's rate ring. A determinate ProgressBar is the only way to
     draw an arc in RemoteViews: the level-driven ring below is swept by
     setProgressBar, rotated so it starts at twelve o'clock. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape
            android:innerRadiusRatio="2.6"
            android:shape="ring"
            android:thicknessRatio="11"
            android:useLevel="false">
            <solid android:color="@color/tide_blossom_shoal" />
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <rotate
            android:fromDegrees="270"
            android:pivotX="50%"
            android:pivotY="50%"
            android:toDegrees="270">
            <shape
                android:innerRadiusRatio="2.6"
                android:shape="ring"
                android:thicknessRatio="11"
                android:useLevel="true">
                <solid android:color="@color/tide_blossom_lantern" />
            </shape>
        </rotate>
    </item>
</layer-list>
