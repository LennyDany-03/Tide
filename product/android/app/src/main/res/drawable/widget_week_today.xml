<?xml version="1.0" encoding="utf-8"?>
<!-- Today, not logged yet: an open square, because an unlogged today is not
     a miss yet. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/tide_cell_empty" />
    <stroke
        android:width="1.4dp"
        android:color="@color/tide_lantern" />
    <corners android:radius="3dp" />
</shape>
