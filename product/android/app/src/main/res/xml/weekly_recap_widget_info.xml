<?xml version="1.0" encoding="utf-8"?>
<!-- Places at 4x2 and resizes both ways; below 160dp tall the day strip
     drops out before the ring is squeezed. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="120dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/widget_weekly_recap"
    android:previewLayout="@layout/widget_weekly_recap_preview"
    android:previewImage="@drawable/widget_preview_fallback"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/widget_recap_description">
</appwidget-provider>
