<?xml version="1.0" encoding="utf-8"?>
<!-- Places at 4x2 and resizes both ways; the list scrolls, so a short
     widget still reaches every row. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/widget_today_habits"
    android:previewLayout="@layout/widget_today_habits_preview"
    android:previewImage="@drawable/widget_preview_fallback"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/widget_today_description">
</appwidget-provider>
