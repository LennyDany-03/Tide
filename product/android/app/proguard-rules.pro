# Tide — release shrinking rules.
#
# Nothing here is needed while `isMinifyEnabled` is false, which is how this
# app builds today. The Razorpay keep rules that used to live here went with
# the payment SDK: Tide is free and ships no checkout.
